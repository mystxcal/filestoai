[package]
name = "filestoai-core"
description = "Walk a project, classify what is in it, and render it as one block of context for a language model."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
ignore = "0.4"
memchr = "2"
rayon = "1"
serde = { workspace = true }
